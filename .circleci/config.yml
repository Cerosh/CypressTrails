version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/run:
          name: update npm
          command: "npm run test-junit"
          post-steps:
            - store_test_results:
                path: junit-results
            - store_artifacts:
                path: cypress/screenshots
            - store_artifacts:
                path: cypress/videos